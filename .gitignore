# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
crash.log

# Local build artifacts — never commit a cluster password or a connection env
*.log
cymbalgoal-conn.env
manifest.json

# Workload generator runtime state
workload/.pids
workload/out/

# Python / notebook noise
.ipynb_checkpoints/
__pycache__/
.venv/
.DS_Store

# internal build tooling — not for students
build/
lint_md.py
